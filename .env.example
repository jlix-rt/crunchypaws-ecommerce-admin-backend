# Log de peticiones HTTP en consola. Silenciar: HTTP_LOG=0
# HTTP_LOG=0

PORT=4001
DATABASE_URL=postgresql://user:pass@localhost:5432/crunchypaws-ecommerce
JWT_SECRET=change-me-same-as-shop-or-dedicated-admin-secret-min-32
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=
ADMIN_FRONTEND_URL=http://localhost:4201
# Raíz pública del admin (HTTPS en prod) para el enlace del correo de verificación. Si se omite, se usa ADMIN_FRONTEND_URL.
# ADMIN_VERIFICATION_FRONTEND_BASE_URL=https://admin.tu-dominio.com
# Origen API tienda sin /api (rutas de imágenes usan /uploads en la raíz del mismo host).
PUBLIC_SHOP_API_URL=http://localhost:4000
STORAGE_PATH=../crunchypaws-ecommerce-backend/uploads
